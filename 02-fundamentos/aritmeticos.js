const [ a, b, c, d] = [ 3, 5, 2, 19]

const soma = a + b + c + d
const subtracao = a - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, divisao, multiplicacao, modulo)